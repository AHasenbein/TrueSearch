services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  grobid:
    image: lfoppiano/grobid:0.8.0
    restart: unless-stopped
    ports:
      - "8070:8070"

volumes:
  mongo_data:
